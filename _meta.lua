local _ = require("gettext")
return {
    fullname = _("Folder Memory"),
    description = _([[Remembers per-folder settings: sort order, display mode (list/mosaic/classic),
book status filter, and items per page (mosaic grid / list).
Restores saved settings when returning to a folder.]]),
}